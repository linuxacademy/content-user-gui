extends layout

block content
  table(class='table table-striped table-condensed table-hover')
    thead
      tr
        th Name:
        th Username:
        th Email:
    tbody
      each user in users
        tr
          td #{user.first_name} #{user.last_name}
          td #{user.username}
          td #{user.email}
